# 미정 항목 추적

사내에 확인해야 할 것들. 답이 오는 대로 여기 채우고
`prompts.md` §6 변경 전파 루프를 돈다 (계약 → fixture → 컴포넌트 순).

## 우선순위 높음 — Phase 6 시작 전에 답이 필요

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 1 | 평가 대상 범위 — API 1개인가, 서비스 전체인가 | 화면 구조가 통째로 바뀐다 | **확정 (2026-07-23)** | **DAC 앱 단위.** DAC 이 앱마다 Swagger 를 생성하므로 Swagger 1개 = 앱 1개, 엔드포인트 1개 = 등록된 SELECT 쿼리 1개다. `target` 을 앱 필드로 바꾸고 쿼리별 통계 `queries` 를 신설했다 (contract.md §0, §2) |
| 2 | 질문 100개 생성 주체 — LLM 생성인가 사람 검수인가 | 파이프라인 설계 + `meta.questionSource` | 미정 | |
| 3 | 실패 원인 분류를 LLM으로 할지 룰베이스로 할지 | `failureCategory` 산출 방식 | 미정 | |

## Port 교체 대상 — 사내 이식 시 반드시 바뀜

| # | 항목 | 대응 Port | 상태 | 답변 |
|---|---|---|---|---|
| 4 | SSO 인증 방식 (토큰 형태, 검증 엔드포인트) | `ports/auth.py` | 미정 | |
| 5 | Swagger 원본 저장 위치 (DB? 파일? 사내 API?) | `ports/spec_repository.py` | 미정 | |
| 6 | 사내 LLM 엔드포인트 스펙 | `ports/llm.py` | 제공받음, 스펙 확인 필요 | |
| 7 | 임베딩 모델 제공 여부 (bge-m3 등) | `ports/llm.py` | 미정 | |

## DAC 도메인 확정에 따른 신규 (2026-07-23)

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 50 | DAC 이 실제로 쓰는 HTTP 메서드 | **SELECT 전용이면 전부 GET 일 수 있다.** 그러면 `METHOD_MISMATCH` 분류가 성립하지 않고(메서드가 하나뿐이라 틀릴 일이 없다), `lib/httpMethod.ts` 의 색 구분도 무의미해진다. 실패 원인 enum 과 뱃지 설계가 함께 바뀐다 | 미정 | |
| 51 | 중복 쿼리 탐지가 이번 범위인지 | 같은 데이터를 뽑는 쿼리가 앱 안에 여럿 등록되는 상황. 유사도 검색을 쓴다는 점은 같지만 **인식률 평가와는 다른 화면**이다. 범위에 넣으면 `queries` 에 중복 후보 필드가 붙고 화면이 하나 더 생긴다 | 미정 | |
| 52 | 재생성 요청을 받는 팀의 API 스펙 | `POST /api/v1/specs/{app_id}/regenerate-request` 가 무엇을 보내고 무엇을 돌려받는지. contract.md §4 는 이쪽에서 필요한 최소치로 적어둔 초안이다. 확정되면 계약을 먼저 고친다 | 미정 | |
| 53 | `needsRegeneration` 판정 기준을 누가 정하는가 | 백엔드 룰(등급 CRITICAL 이면 후보)인지, 담당자가 임계값을 조정하는지. **fixture 는 등급 CRITICAL 기준으로 채웠다.** 담당자 조정식이면 화면에 임계값 입력이 필요하고, 그러면 `'use client'` 컴포넌트가 처음으로 생긴다 | 미정 | |

## 담당자 확정 스펙(2026-07-24)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 54 | Top-1 과 Top-3 의 등급 임계값이 같은가 | `summary.top1Grade` / `top3Grade` 를 분리했고 `lib/gradeBands.ts` 도 지표별 구조로 나눌 예정. **현재는 둘 다 0-70-85-95 로 동일**하게 채웠지만, Top-1 은 더 엄격해야 한다는 의견이 있으면 임계값이 갈린다 | 미정 | |
| 55 | 평가툴 출력 포맷 — DB 테이블인가 JSON 인가, 컬럼 정의 | 백엔드의 1차 역할이 평가툴 출력을 계약으로 변환하는 어댑터다(contract.md §0). 입력 포맷이 정해져야 어댑터를 쓸 수 있다. `meta.rawSource` 는 그 원본 버전을 남기는 자리 | 미정 | |
| 56 | 평가툴을 이 백엔드에 통합할 가능성 | 지금은 외부 툴이 결과를 만들고 이 시스템은 표시·전달만 한다. 담당자가 통합을 요청하면 질문 생성·검색·채점이 `services/` 안으로 들어오고 Port(LLM/Embedder) 소비자가 생긴다 | 담당자 요청 시 | |
| 57 | 100문항 원본 질문을 화면에 전부 노출해도 되는지 | `questions[].question` 은 현장 언어 질문 100개다. 실제 운영에서는 공정·설비를 가리키는 표현이 섞일 수 있어 그대로 노출하면 정보가 샐 여지가 있다. 마스킹·권한 게이팅이 필요한지 보안 검토 | 보안 검토 필요 | |

## 우선순위 중간

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 8 | 평가 이력 보관 기간 / 재실행 주기 | `previous` 델타, 추이 차트 | 미정 | |
| 9 | "Swagger 재생성" 결과 반영 방식 (PR 생성? 직접 수정?) | ActionPanel 동작 | 미정 | |
| 10 | 재생성이 기존 description을 덮어쓸 때 롤백 수단 | 파괴적 동작 안전장치 | 미정 | |
| 11 | 사내 Nexus에 `openapi-typescript` 존재 여부 | 프론트 타입 생성 방식 | 불필요 | 생성물 api-types.ts를 저장소에 커밋하기로 결정(2026-07-22). 사내에선 파일만 복사하면 되므로 도구 자체는 필요 없음. 단 계약 변경 시 GitHub 쪽에서 재생성 후 다시 복사 |
| 12 | 폰트 파일 반입 가능 여부 (Pretendard 등) | 디자인 토큰 | 확인 중 — **파일만 오면 즉시 적용 가능** | 코드는 준비 완료(2026-07-22). `src/styles/fonts/` 에 woff2 6개를 넣고 `src/app/fonts.ts` 주석 해제 + `globals.css` 두 줄 교체면 끝. 절차는 `frontend/src/styles/fonts/README.md`. 반입이 불가하면 지금의 시스템 폰트 fallback 을 그대로 유지하면 되고, 화면은 깨지지 않는다 |

## 환경

| # | 항목 | 상태 | 메모 |
|---|---|---|---|
| 13 | 사내 Next.js 16.0.0 → 보안 패치 필요 | 확인 필요 | 16.2.11 Active LTS 권고 |
| 14 | 사내 프로젝트 스타일 = CSS Modules 확정? | 확정 | package.json에 Tailwind/SCSS 없음 |
| 15 | 저장소 public/private 여부 | 미정 | 사내망에서 열람하려면 public이 편함 |
| 34 | Ingress 경로 — 루트인가 서브패스(`/swagger-eval`)인가 | 미정 | `basePath` 결정. **Next 의 basePath 는 런타임에 못 바꾼다** — 이것만 빌드 타임 값이라, 서브패스면 환경별 빌드가 강제된다 |
| 35 | 이미지 승격 방식 — 환경별 빌드인가 단일 이미지 다환경인가 | 미정 | 런타임 env 설계. 단일 이미지를 전제로 `NEXT_PUBLIC_` 을 하나도 쓰지 않았다(#34 만 예외). 환경별 빌드로 간다면 이 제약을 풀 수 있다 |
| 36 | SSO 처리 위치 — Ingress/oauth2-proxy 인가 앱 내부인가 | 미정 | `AuthProvider` 범위(#4). 앞단에서 처리되면 앱은 헤더만 읽으면 되고, 앱 내부면 토큰 검증까지 Port 뒤에 들어온다 |
| 41 | 사내 Python index URL 확정 | 미정 | `UV_DEFAULT_INDEX` 에 넣을 값. PEP 503 경로라 보통 `/simple` 로 끝난다. 값이 없으면 `make setup` 이 PyPI 로 나가려다 실패한다. **URL 을 저장소에 커밋하지 않는다** — 환경변수로만 준다 (`docs/prompts.md` §10-1) |
| 42 | 사내 npm registry URL 확정 | 미정 | `frontend/.npmrc` 의 `registry=`. 커밋된 `package-lock.json` 에는 `registry.npmjs.org` 가 박혀 있어 사내에서 `npm ci` 가 실패한다. 그때는 `npm install` 로 사내 락을 새로 만든다(커밋 금지, §10-2) |
| 43 | 사내 CA 인증서 배포 경로 / `NODE_EXTRA_CA_CERTS` 표준 위치 | 미정 | PC 마다 경로가 다르면 셋업 문서를 쓸 수 없다. 사내 표준 경로가 정해져 있는지, 아니면 각자 내려받는지 확인 필요. Python 쪽은 `UV_SYSTEM_CERTS=true` 로 OS 신뢰 저장소를 쓰면 경로가 필요 없다 |
| 45 | **`BASE_PATH` 를 전 환경 동일하게 통일할지** | 미정 | **#35 와 정면 충돌한다.** Next 의 `basePath` 는 빌드 타임에 굳어서 런타임 env 로 못 바꾼다. 실측: `--build-arg BASE_PATH=/swagger-eval` 로 빌드한 이미지는 `/` 에서 404 다. 환경마다 경로가 다르면 이미지가 환경 수만큼 갈리고, "dev 에서 검증한 이미지를 그대로 prd 로 승격" 이 불가능해진다. **(1) 전 환경 동일 경로로 통일**(권장) 또는 **(2) #35 철회하고 환경별 빌드 수용** 중 하나를 골라야 한다. #34(Ingress 경로)와 함께 결정 |
| 46 | 사내 베이스 이미지 경로 | 미정 | `PYTHON_IMAGE` / `NODE_IMAGE` ARG 에 넣을 값. 폐쇄망에서 Docker Hub 를 못 쓰면 사내 미러의 `python:3.12-slim`, `node:22-alpine` 경로가 필요하다. 태그 정책(고정 버전인지 다이제스트인지)도 함께 확인 |
| 47 | 컨테이너 레지스트리 경로 / 태그 정책 | 미정 | 빌드한 이미지를 push 할 곳. backend/frontend 두 이미지를 **같은 버전 태그로 세트 승격**하는 규칙을 전제로 문서를 썼다(README). 사내 CI 가 이미지를 따로 빌드·승격하는 구조면 계약 버전이 어긋날 수 있다 |
| 44 | Windows 개발 PC 를 공식 지원 대상으로 둘지 | 미정 | 현재는 지원한다는 전제로 Makefile 에서 셸 로직을 걷어냈다(Phase 4.6). WSL2 를 표준으로 정하면 이 제약이 사라지고 POSIX 만 가정할 수 있다. 다만 사내 PC 에 WSL 설치 권한이 있는지가 관건. **git bash 와 cmd 를 섞어 쓰는 지금이 가장 나쁜 상태다** — 한쪽에서만 되는 스크립트가 계속 생긴다 |

## Phase 1(스캐폴딩)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 16 | 사내 프론트의 lint 방식 — Next 16은 `next lint`가 제거됨 | `npm run lint`를 `eslint .` + flat config(`eslint.config.mjs`)로 잡아둠. 사내가 `.eslintrc`를 쓰면 교체 필요 | 미확인 | |
| 17 | 사내 Python 의존성 관리 — uv인가 pip/requirements.txt인가 | `pyproject.toml`을 uv 기준으로 작성. 폐쇄망 Nexus 미러 여부에 따라 달라짐 | 미확인 | |
| 18 | `make setup`이 `npm ci`를 쓰는데 `package-lock.json`이 아직 없음 | 최초 1회는 `npm install`로 lock을 만들어 커밋해야 함 | 조치 필요 | |
| 19 | 대시보드 → 백엔드 호출 주소를 어디서 주입할지 | Phase 7 서버 컴포넌트 fetch. 지금은 백엔드 CORS만 `localhost:3000`으로 열어둠 | 미정 | |
| 20 | `lucide-react` 사내 버전 고정값 | 규칙상 새 패키지 추가 금지 → 사내에 이미 있는 버전과 맞춰야 함 | 미확인 | |

## Phase 2(sample-api)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 21 | 평가 대상 경로에 `/api/v1` 접두사가 붙나 | `contract.md` 37행 target은 `/api/v1/orders/{id}/refund-status`, 84~88행 failures는 `/orders/{id}/...` 로 **서로 다름**. 지시받은 엔드포인트 목록을 따라 접두사 없이 구현함 | 계약 불일치, 확인 필요 | |
| 22 | 등급 정답지를 스펙 안에 넣는 방식 (`x-quality` 확장) | 등급별 인식률 비교(Phase 8 검증 목표)에 ground truth가 필요. 단 **검색 입력으로 쓰면 안 됨** — 평가기는 summary/description만 봐야 한다 | 로컬 방식 확정, 사내 스펙에는 없을 것 | |
| 23 | 사내 Swagger 자동 생성기도 summary를 함수명으로 채우나 | FastAPI는 summary 미지정 시 함수명으로 자동 생성한다(`delete_refund` → "Delete Refund"). 진짜 EMPTY를 재현하려고 `custom_openapi()` 에서 제거함. 사내 생성기가 같은 동작이면 **현실의 EMPTY는 존재하지 않고 전부 POOR** 이며, 평가 난이도가 통째로 달라진다 | 확인 필요 | |
| 24 | 평가 코퍼스에서 시스템 엔드포인트 제외 규칙 | `/health` 를 `include_in_schema=False` 로 스펙에서 뺐다. 사내 스펙에 헬스체크·내부 API가 섞여 있으면 제외 규칙이 필요 | 미정 | |

## Phase 3(응답 계약)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 25 | `meta.questionSource` 를 enum 으로 고정할지 | 계약 §3 enum 목록에 없어 자유 문자열로 뒀다. fixture 값은 `LLM_GENERATED_HUMAN_REVIEWED`. 화면에 신뢰도 근거로 노출되므로 값이 흔들리면 문구가 깨진다. #2(문항 생성 주체)와 같이 결정된다 | 미정 | |
| 26 | `results[].score` 의 범위 | 정규화 가정으로 `ge=0, le=1` 제약을 걸었다. 사내 검색이 정규화되지 않은 BM25 점수를 그대로 주면 1을 넘어 검증에서 터진다 | 확인 필요 | |
| 27 | `target.method` / `expected.method` 를 enum 으로 막을지 | 지금은 자유 문자열. HTTP 메서드는 유한하므로 enum 이 안전하지만 계약 §3에 없어 추가하지 않음 | 미정 | |
| 28 | `failures` 페이지네이션 | fixture 는 22건 전량을 내려준다. 실패가 수백 건인 명세에서는 응답이 비대해진다. 지금 테스트는 `len(failures) == top3FailCount` 를 전제로 한다 | 미정 | |
| 29 | 경로 표기 통일 (#21 후속) | fixture 는 `target.path` 를 포함해 전부 `/orders/{id}/...` 로 적었다. contract.md 37행의 `/api/v1/` 접두사와 다르다 | #21 과 함께 결정 | |

## Phase 4(Port 격리)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 30 | `SpecRepository` 를 동기로 둘지 async 로 둘지 | 로컬은 파일 읽기라 동기가 맞지만, 사내가 DB/HTTP 라면 `async def` 여야 이벤트 루프를 막지 않는다. **나중에 바꾸면 라우터까지 전부 async 로 번진다** — Port 시그니처 중 가장 되돌리기 비싼 결정 | 확인 필요 | |
| 31 | `get_spec(spec_id)` 의 반환 타입 | 지금은 파싱된 `dict[str, Any]`. 사내가 원문 문자열이나 DB 레코드를 준다면 파싱 책임 위치가 바뀐다 | 미정 | |
| 32 | `AuthProvider.get_current_user()` 가 요청 컨텍스트를 어떻게 받나 | 지금 시그니처는 인자가 없다. SSO 토큰을 헤더에서 꺼내야 하면 `Request` 나 토큰 문자열을 받는 형태로 바뀐다. 소비자가 없어 미확정인 채로 뒀다 (#4) | 미정 | |
| 33 | 평가 결과 저장(쓰기) 경로 | 지금 Port 는 읽기 전용(`get_evaluation`/`get_spec`). Phase 8 에서 평가 결과를 저장하려면 `save_*` 가 필요하고, 그때 사내 저장소가 쓰기를 허용하는지가 걸린다 | 미정 | |

## Phase 5(디자인 토큰)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 37 | `priority` 의 색 지정 | `contract.md` §3 은 grade 색만 정의한다. priority 는 지정이 없었다 | **확정 (2026-07-22)** | `HIGH→--red`, `MEDIUM→--sky`, `LOW→--text-mute`. 근거는 시안의 권장조치 카드가 red/sky 2단계로만 구분한다는 점. **amber 는 grade 전용**으로 남긴다 — priority 에 쓰면 화면에서 "개선 필요(등급)"와 "MEDIUM(우선순위)"이 같은 색이 되어 무엇을 가리키는지 알 수 없다. LOW 는 색을 빼서 시선을 끌지 않게 했다 |
| 38 | 등급 라벨 문구 — `심각/개선 필요/보통/우수` 인가 `미흡/주의/양호/우수` 인가 | `contract.md` §3 표와 `prompts.md` §9-2 문구 수정안이 서로 달랐다 | **확정 (2026-07-22)** | `contract.md` §3 의 **심각/개선 필요/보통/우수** 유지. `prompts.md` §9-2 의 변경 제안은 철회하고 해당 행을 삭제했다. 라벨 문자열은 `frontend/src/lib/enumTokens.ts` 의 `gradeLabel` 한 곳에만 존재한다 |
| 39 | 질문 유형 7종의 색 | 도넛 7조각을 채워야 하는데 시안 강조색은 5개뿐이었다 | **확정 (2026-07-22)** | 원색 2개 추가(`--indigo #818CF8`, `--pink #F472B6`). **상태색과 분리해 `--chart-type-*` 접두사로 신설.** direct=sky / user-nl=violet / domain-term=green / parameter=amber / error-case=red / short-keyword=indigo / mixed-lang=pink. 범주 구분용이며 위험·주의를 뜻하지 않는다 |
| 49 | `loading.tsx` 를 유지할지 — 스켈레톤 vs 정확한 상태 코드 | **둘 중 하나만 된다.** `loading.tsx` 가 Suspense 경계를 만들어 스트리밍이 시작되면, 응답 상태가 이미 200 으로 확정된 뒤에 `notFound()`/에러가 발생한다. 실측: `loading.tsx` 있으면 `/eval/NOPE` → **200**(화면은 not-found 가 맞게 뜸), 없으면 → **404**. 백엔드가 죽었을 때도 있으면 200, 없으면 500. 지금은 지시대로 `loading.tsx` 를 유지했다(평가가 수십 초 걸려 진행 중 화면이 가장 자주 보인다 — §9-4). 모니터링·uptime 체크가 상태 코드를 본다면 Phase 7 에서 재검토 필요 | Phase 7 에서 결정 | |
| 48 | 사내 브라우저 버전 — `color-mix()` 지원 여부 | `SummaryCards` 가 카드 배경·테두리 투명도에 `color-mix(in srgb, var(--accent) 9%, transparent)` 를 쓴다. Chrome/Edge 111+, Safari 16.2+ 필요. 미지원이면 카드가 색 없이 `--surface` 배경으로 보인다(레이아웃은 안 깨지도록 폴백 선언을 앞에 뒀다). 사내 표준 브라우저가 그보다 낮으면 색 단계마다 토큰을 따로 정의해야 한다 | 확인 필요 | |
| 40 | 폰트 파일 경로와 `BASE_PATH` 충돌 | `public/` 정적 파일에는 Next 가 basePath 접두사를 붙여주지 않아, 서브패스 배포(#34)면 `/fonts/...` 가 404 가 된다 | **해결 (2026-07-22)** | `public/fonts/` 를 없애고 `src/styles/fonts/` 로 옮겼다. `next/font/local`(`src/app/fonts.ts`)이 번들러를 거치므로 basePath 를 바꿔도 깨지지 않는다. 폰트 파일이 아직 없어 설정은 주석 처리 상태이며, `globals.css` 는 `--font-sans`/`--font-mono` 만 보므로 파일이 오면 두 줄만 고치면 된다 |

## Phase 8(라이트 모드)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 58 | 런타임 테마 토글에 localStorage 를 써도 되는지 | Phase 8 에서 라이트 모드를 `prefers-color-scheme` 자동 전환으로만 넣고, 수동 토글은 "유저 설정 저장 부담"을 이유로 미뤘다 | **확정 (2026-07-26)** | **써도 된다. Phase 8a 에서 3단계(auto/light/dark) 토글을 추가했다.** localStorage 1키(`theme`)는 **서버 저장이 아니라 브라우저 로컬**이라 DB·유저관리가 필요 없다 — 미뤘던 근거가 성립하지 않는다. FOUC 방지용 인라인 스크립트(layout.tsx `<head>`) 1개와 `'use client'` 컴포넌트 1개(`components/common/ThemeToggle`)만 새로 생겼고, 나머지 서버 컴포넌트 구조는 그대로다. auto 는 data-theme 를 비워 CSS 가 OS 를 실시간으로 따른다 |

## Phase 10(안정화)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 59 | **평가툴이 계약을 어겼을 때 화면에 태울지, 걸러낼지** | Phase 10 에서 `services/adapter.py` 가 필수 필드 누락·형식 오류를 `ContractViolation` 으로 터뜨리고, API 가 500 + 어느 필드가 틀렸는지를 내려주도록 했다. **한 필드가 틀리면 리포트 전체가 안 보인다.** 담당자가 "숫자 하나 빠져도 나머지는 보고 싶다" 고 하면 정책이 반대로 뒤집힌다(부분 렌더 + 결손 표시). 지금은 조용한 오표시보다 명시적 실패가 낫다고 보고 전자를 골랐다 | 정책 확인 필요 | |
| 60 | 서버 컴포넌트 fetch 타임아웃 8초가 맞는지 | `lib/api.ts` 의 `REQUEST_TIMEOUT_MS`. 저장된 결과를 읽어 오는 조회라 8초를 넉넉히 봤지만, 사내가 DB 어댑터로 바뀌고 100문항을 조인해 오면 모자랄 수 있다. 넘기면 error.tsx 가 뜬다 | 사내 저장소 확정 후 재측정 | |
| 61 | 목록에서 깨진 파일을 건너뛰는 것이 맞는지 | `FileSpecRepository.list_evaluations` 는 형식이 깨진 결과를 **경고 로그를 남기고** 건너뛴다. 하나 때문에 목록 전체가 막히지 않게 한 것이다. 다만 사용자에게는 "그런 평가가 없는 것" 과 구분되지 않는다. 목록 응답에 "읽지 못한 N건" 을 실을지 결정 필요 | 미정 | |
| 62 | 경계값 fixture 를 사내에도 들고 갈지 | `app/scripts/make_edge_fixtures.py` 가 만드는 6종(E100/ELOW/E1Q/E3T/EFIRST/ELONG)은 화면 검증용이다. 사내 저장소가 DB 라면 이걸 넣을 자리가 없다 — 그 경우 화면 경계 검증을 어디서 할지(스테이징 데이터? 목 어댑터?) 정해야 한다 | 미정 | |
| 63 | `/eval/{id}` 의 HTTP 상태 코드 (#49 후속) | `loading.tsx` 를 유지했으므로 없는 traceId 도 **200** 이다(화면은 not-found 가 맞게 뜬다). Phase 10 에서 스켈레톤을 실제 레이아웃에 맞춰 더 키웠기 때문에 이걸 버리는 선택은 더 비싸졌다. 모니터링이 상태 코드를 본다면 `/api/health` 같은 별도 경로를 보게 하는 편이 낫다 | #49 를 이 방향으로 유지 | |

## Phase 11(등장 애니메이션 + 접근성)에서 생긴 것

| # | 항목 | 왜 필요한가 | 상태 | 답변 |
|---|---|---|---|---|
| 64 | **시안 색 `--text-mute` 를 바꿨다 — 승인 필요** | 다크 `#64748b` 는 `--surface` 위에서 **3.73:1** 로 WCAG AA 본문 기준(4.5:1)에 미달했다. 이 토큰은 전부 작은 글씨(구간 범위, 단위, 각주, 메타 라벨)에 쓰여 대비가 가장 필요한 자리다. `#748399` 로 한 단계 밝혀 4.60:1(surface) / 5.23:1(bg) 을 만들었다. **CLAUDE.md 규칙 5 의 "원색은 시안 기준, 임의 변경 금지" 를 건드린 유일한 항목**이라 별도로 남긴다. 되돌리려면 AA 미달을 수용하는 결정이 필요하다 | **디자인 승인 필요** | |
| 65 | 라이트 모드 등급색을 어둡게 했다 — 차트 선명도와의 교환 | 등급색은 **텍스트(등급 라벨 12~15px)와 차트 채움 양쪽에** 쓰인다. 라이트 모드 값이 흰 배경에서 amber 3.19:1 / green 3.30:1 / sky 4.10:1 로 텍스트 기준 미달이었다. 원색을 한 단계 어둡게 해(`#b45309` / `#15803d` / `#0369a1` / `#b91c1c` / `#be185d`) 전부 4.5:1 이상으로 올렸다. **대신 라이트 모드 차트가 조금 탁해진다.** 차트 채움만 보면 비텍스트라 3:1 로 충분하므로, 선명도를 되찾으려면 `--grade-*-text` 같은 텍스트 전용 변형을 새 토큰 층으로 두면 된다 — 다만 "텍스트 자리에서는 반드시 그 변형을 쓸 것" 이라는 규칙이 하나 늘어난다. 지금은 팔레트 하나로 어디서나 안전한 쪽을 골랐다(파일 단위로 복사되는 저장소라 규칙이 적을수록 안전하다) | 담당자 확인 | |
| 66 | 애니메이션 시작 상태가 "보이지 않음" 인 것에 대한 의존 | 등장 애니메이션은 전부 `animation-fill-mode: both` 이고 `from` 이 투명·폭0 이다. 그래서 **애니메이션이 시작되기 전에는 요소가 안 보인다.** 정상 경로(첫 페인트 = t0)에서는 이게 맞고, `prefers-reduced-motion` 도 검증했다(즉시 최종 상태). 다만 사내 표준 브라우저가 `@keyframes` 안의 개별 `translate` 속성을 모르면 이동만 빠지고 페이드는 정상 동작한다 — 확인은 #48(color-mix)과 같이 하면 된다. `color-mix()` 가 더 최신 기능이라 이쪽이 먼저 문제가 될 가능성은 낮다 | #48 과 함께 확인 | |
| 67 | 비활성 체크박스는 Tab 으로 갈 수 없다 | Phase 11 접근성 점검 항목에 "체크박스 포커스 링" 이 있었으나, 그 체크박스는 `disabled` 라 애초에 포커스를 받지 않았다(브라우저 규약) | **해소 (2026-07-30)** | 체크박스가 있던 `QueryQualityTable` 자체가 사라졌다 — 여러 쿼리 중 골라 재생성 요청하는 흐름이 없어졌기 때문이다(#1, #53). 포커스 링은 전역 `:focus-visible` 하나가 남은 인터랙티브 요소 전부를 덮는다 |

---

## 기록 규칙

- 답을 받으면 **상태를 `확정`으로 바꾸고 답변 칸을 채운다**
- 계약에 영향이 있으면 `contract.md` §6 변경 이력에도 남긴다
- 사내에서 먼저 코드를 고치지 않는다. 계약이 항상 먼저다
